# 🟣 Блок 5: Внутреннее устройство Redis (Internals)

### 16. Алгоритмы вытеснения (Eviction Policies)

Когда память (`maxmemory`) заканчивается, система кэширования должна удалить что‑то старое, чтобы записать новое.

1. **LRU (Least Recently Used):**  
* *Логика:* Удаляем то, к чему **дольше всего не обращались**.  
* *Принцип:* Если ключ давно не использовали — он считается «холодным».  
* *Реализация:* Обычно используется приблизительный LRU, чтобы не хранить полный список обращений.

2. **LFU (Least Frequently Used):**  
* *Логика:* Удаляем ключи с **минимальным количеством обращений**.  
* *Разница:* Популярные ключи живут дольше, даже если ими давно не пользовались.  
* *Use Case:* Хорошо подходит для долгоживущего контента.

3. **FIFO (First In, First Out):**  
* *Логика:* Удаляем **самый первый вставленный** ключ.  
* *Принцип:* Порядок вставки важнее частоты и давности использования.  
* *Use Case:* Простые очереди, буферы, предсказуемое вытеснение.

4. **LIFO (Last In, First Out):**  
* *Логика:* Удаляем **последний вставленный** ключ.  
* *Принцип:* Противоположность FIFO — «последний пришёл, первый ушёл».  
* *Use Case:* Редко используется, но подходит для стекоподобных структур.

5. **MRU (Most Recently Used):**  
* *Логика:* Удаляем то, к чему **обращались совсем недавно**.  
* *Принцип:* Если данные только что использовали — значит, они уже «отработали».  
* *Use Case:* Подходит, когда «горячие» данные быстро устаревают (например, временные результаты вычислений).

6. **Random:**  
* *Логика:* Удаляем **случайный ключ**.  
* *Принцип:* Максимально быстрый алгоритм без вычислений.  
* *Use Case:* Когда все ключи примерно равнозначны и важна скорость.

---

### 17. Персистентность: RDB vs AOF

Redis работает в RAM, но данные нужно сохранять на диск, чтобы пережить рестарт.

1. **RDB (Redis Database): Снапшоты.**
* *Как работает:* Раз в X минут (например, раз в 5 минут) Redis делает **Fork** процесса и сохраняет *все* данные в компактный файл `.rdb`.
* *Плюсы:* Файл маленький, Redis стартует с него очень быстро. Идеально для бэкапов.
* *Минусы:* **Потеря данных.** Если сервер упадет через 4 минуты 59 секунд после снапшота — вы потеряете данные за эти 5 минут.


2. **AOF (Append Only File): Журнал операций.**
* *Как работает:* Каждая команда записи (`SET`, `INCR`) дописывается в конец файла лога.
* *Настройка `fsync`:* Обычно ставят `everysec` (сбрасывать на диск каждую секунду).
* *Плюсы:* Надежность. Максимум 1 секунда потери данных.
* *Минусы:* Файл растет бесконечно (нужен механизм Rewrite/Compact), старт сервера медленнее (нужно проиграть весь лог заново).



**Best Practice:** Включать **оба** (Hybrid Persistence). RDB для быстрых бэкапов, AOF для надежности.

---

### 18. Pipelining (Пайплайнинг)

Это оптимизация сети, а не самого Redis.

* **Проблема:** Redis обрабатывает запрос за наносекунды, но сетевой путь туда-обратно (RTT — Round Trip Time) занимает миллисекунды.
* `Клиент -> (10мс) -> Сервер -> (10мс) -> Клиент` = 20мс на команду.


* **Решение:** Отправить пачку команд (100 штук) **одним пакетом**, не дожидаясь ответа на каждую.
* `Клиент -> (10мс с пачкой команд) -> Сервер выполняет все -> (10мс с пачкой ответов) -> Клиент`.


* **Результат:** Ускорение в 10-50 раз при массовых вставках.

---

### 19. Lua-скрипты

Это "супер-оружие" для атомарности.

* **Зачем:** Иногда нужно сделать логику: *"Прочитай значение, если оно равно X, то удали его, иначе верни ошибку"*.
* **Проблема:** Если делать это двумя командами (`GET` + `DEL`) с клиента, между ними может вклиниться другой клиент (Race Condition).
* **Решение:** Написать скрипт на Lua и отправить его в Redis.
* **Гарантия:** Redis выполняет скрипт **атомарно**. Пока скрипт работает, **никакие другие команды не выполняются**. Скрипт блокирует весь сервер, поэтому он должен быть очень быстрым.

---

### 20. Транзакции (`MULTI` / `EXEC`)

На собеседовании тут ловят тех, кто привык к SQL.

1. **Как это выглядит:**
* `MULTI` (начало транзакции).
* `SET a 1` (команда ставится в очередь, возвращается `QUEUED`).
* `INCR b` (в очередь).
* `EXEC` (выполнить всё скопом).


2. **В чем подвох (Отличие от SQL):**
* В SQL: Если одна команда упала с ошибкой, вся транзакция откатывается (**Rollback**).
* В Redis: Если одна команда упала (например, сделали `INCR` над строкой), **остальные всё равно выполнятся!** В Redis **нет Rollback**.


3. **WATCH (Оптимистичная блокировка):**
* Команда `WATCH key` следит за ключом. Если кто-то другой изменит этот ключ *до* того, как вы вызовете `EXEC`, ваша транзакция отменится целиком.

---