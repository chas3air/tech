# 🟢 Блок 1: Архитектурные паттерны и Стратегии

Это фундамент. На собеседовании важно не просто назвать паттерн, а объяснить **Trade-offs** (компромиссы).

### 1. Основные паттерны кэширования

#### **Cache-Aside (Lazy Loading)** — *Самый популярный*

Приложение управляет данными. Кэш — сбоку.

* **Как работает:**
1. Приложение идет в Кэш.
2. Если **Miss** (пусто) — Приложение идет в БД.
3. Приложение берет данные, кладет в Кэш и отдает клиенту.


* **Плюсы:**
* В кэше только "горячие" данные (то, что реально запрашивают).
* Отказоустойчивость: если Redis упадет, приложение просто начнет ходить в базу (хоть и медленнее).


* **Минусы:**
* Первый запрос всегда медленный (Cold start).
* Возможен рассинхрон (кто-то обновил БД напрямую, а в кэше старье).



#### **Write-Through (Сквозная запись)**

Кэш — главный посредник при записи.

* **Как работает:**
1. Приложение пишет данные в Кэш.
2. Кэш синхронно пишет данные в БД.
3. Возвращает "ОК" только когда записалось и там, и там.


* **Плюсы:**
* Полная согласованность данных (Strong Consistency). В кэше всегда свежее.
* Данные не потеряются (т.к. пишем в БД сразу).


* **Минусы:**
* Медленная запись (время = Latency Кэша + Latency БД).
* В кэше могут оседать данные, которые никто никогда не прочитает (записали и забыли).



#### **Write-Back (Write-Behind)**

Асинхронная запись.

* **Как работает:**
1. Приложение пишет в Кэш и сразу получает "ОК".
2. Кэш в фоне сбрасывает данные в БД (например, раз в секунду или пачками).


* **Плюсы:**
* Молниеносная запись. Идеально для счетчиков (лайки, просмотры).
* Снижает нагрузку на БД (превращает 1000 записей в одну пачку).


* **Минусы:**
* **Риск потери данных:** Если кэш упадет до сброса в БД — данные исчезли навсегда.



---

### 2. Выбор стратегии (Read-heavy vs Write-heavy)

* **Read-heavy (Много читаем, редко пишем):** новостной сайт, каталог товаров.
* *Выбор:* **Cache-Aside**.
* *Почему:* Мы хотим кэшировать только то, что читают. При редких обновлениях можно просто инвалидировать кэш (`DEL`).


* **Write-heavy (Много пишем):** сбор логов, счетчики лайков, трекинг координат такси.
* *Выбор:* **Write-Back** (если допустима потеря данных) или **Redis как основное хранилище** (если нужна скорость).
* *Нюанс:* Cache-Aside здесь будет работать плохо, так как при каждой записи мы будем инвалидировать кэш, и следующий читатель снова пойдет в базу. Кэш будет постоянно "мигать".



---

### 3. Local Cache vs Distributed Cache

#### **Local Cache (In-Memory)**

Хранение данных внутри процесса приложения (например, `HashMap` в Java или `dict` в Python).

* **Скорость:** Наносекунды (доступ к RAM, нет сети).
* **Когда использовать:**
* Статические данные (конфиги, справочники стран).
* Огромная частота запросов, где даже сетевой поход в Redis (1-2мс) — это долго.


* **Проблема:**
* **Рассинхронизация:** Если у вас 10 микросервисов, на одном сервере в кэше может лежать цена 100, а на другом — 200.
* **Память:** Дублирование данных в памяти каждого инстанса.



#### **Distributed Cache (Redis/Memcached)**

Внешний сервер.

* **Скорость:** Миллисекунды (TCP/IP накладные расходы).
* **Когда использовать:**
* Данные, которые часто меняются (сессии пользователей, корзина, баланс).
* Нужен единый источник правды для всех серверов.
* Данные должны пережить перезапуск вашего приложения.



---

**Готов переходить к Блоку 2 (Проблемы и Сбои)?** Там про Avalanche, Penetration и Stampede.