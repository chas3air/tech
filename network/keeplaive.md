Разница между **TCP keepalive** и **gRPC keepalive**
---

# 🔹 TCP Keepalive — механизм уровня транспорта (L4)

**TCP keepalive** — это низкоуровневый механизм ядра ОС, который проверяет, живо ли TCP‑соединение, когда по нему долго нет трафика.

### Как работает:

- TCP-соединение простаивает.
- Через `tcp_keepalive_time` (обычно **2 часа** по умолчанию в Linux) ядро отправляет **пустой пакет** (probe).
- Если нет ответа — повторяет `tcp_keepalive_probes` раз.
- Если все попытки провалены — соединение считается мёртвым.

### Для чего нужен:

- Выявлять «мертвые» соединения (например, когда клиент умер без FIN/RST).
- Освобождать ресурсы ядра.
- Поддерживать NAT‑таблицы живыми (иногда).

### Особенности:

- Работает **только на уровне TCP**, ничего не знает о приложении.
- Интервалы огромные (часы), потому что это механизм для сетевой стабильности, а не для приложений.
- Не гарантирует, что приложение живо — только что TCP‑сокет не умер.

---

# 🔹 gRPC Keepalive — механизм уровня приложения (L7)

**gRPC keepalive** — это *прикладной* механизм поверх HTTP/2, который проверяет, жив ли peer и не закрыл ли соединение прокси/NAT.

Он работает **внутри HTTP/2**, а не в TCP.

### Как работает:

- Клиент периодически отправляет **HTTP/2 PING** кадры.
- Сервер отвечает **ACK**.
- Если ACK нет — gRPC закрывает соединение и пересоздаёт канал.

### Для чего нужен:

- Быстро обнаруживать разрыв соединения (секунды, а не часы).
- Поддерживать соединение живым через NAT/Firewall, которые режут idle‑трафик.
- Управлять политикой нагрузки (например, сервер может запретить слишком частые keepalive).

### Особенности:

- Интервалы — секунды или десятки секунд.
- gRPC может закрыть соединение, даже если TCP ещё жив.
- Позволяет серверу задавать политику:  
  `permit_without_calls`, `max_connection_age`, `max_connection_idle`.

---

# 🔥 Главная разница

| Свойство | TCP Keepalive | gRPC Keepalive |
|---------|----------------|----------------|
| Уровень | L4 (TCP) | L7 (HTTP/2, gRPC) |
| Кто реализует | Ядро ОС | gRPC-библиотека |
| Формат | TCP probe (пустой пакет) | HTTP/2 PING frame |
| Интервалы | Минуты–часы | Секунды |
| Цель | Проверить, жив ли TCP-сокет | Проверить, жив ли peer и канал gRPC |
| Видит ли приложение | Нет | Да |
| NAT/Firewall | Может не помочь | Помогает удерживать соединение |
| Может ли сервер управлять | Нет | Да (ограничения keepalive) |

---
