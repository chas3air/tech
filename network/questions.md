# 🌐 Блок 1: Сети и HTTP (Networking)

### 1. Жизненный цикл запроса (`google.com`)

Этот вопрос проверяет понимание всех слоев сразу.

**Ответ:**

1. **Parsing:** Браузер парсит URL, определяет протокол (https) и домен.
2. **DNS Lookup (Резолвинг IP):**
* Браузер смотрит свой кэш -> Кэш ОС -> Файл `/etc/hosts`.
* Если нет — запрос идет к **DNS Resolver** (обычно у провайдера или 8.8.8.8).
* Резолвер проходит по цепочке: Root (`.`) -> TLD (`.com`) -> Authoritative (`google.com`) и получает IP-адрес.


3. **Socket & TCP Handshake:** Браузер открывает TCP-сокет на порт 443. Происходит **3-way handshake** (SYN, SYN-ACK, ACK). Установлено соединение.
4. **TLS Handshake:** Так как это HTTPS, поверх TCP происходит обмен сертификатами и генерация сессионных ключей (шифрование).
5. **HTTP Request:** Браузер формирует текстовый (или бинарный в HTTP/2) пакет: `GET / HTTP/2`, заголовки (`Host`, `User-Agent`, `Cookie`).
6. **Server Processing:** Балансировщик принимает запрос, отдает бэкенду, бэкенд идет в БД, генерирует HTML.
7. **HTTP Response:** Сервер отдает ответ (Status `200 OK`, Headers, Body).
8. **Rendering:** Браузер строит DOM-дерево и отрисовывает страницу.

---

### 2. Модель OSI и Балансировка (L4 vs L7)

**Модель OSI (упрощенно для Web):**

* **L3 (Network):** IP. Маршрутизация пакетов (адресация).
* **L4 (Transport):** TCP/UDP. Порты, надежность, порядок доставки.
* **L7 (Application):** HTTP, FTP, gRPC. Само содержимое запроса.

**Балансировщики:**

* **L4 Load Balancer (Transport Level):**
* Работает с IP и Портами.
* *Принцип:* Видит пакет "с IP А на порт 80", пересылает его на "Сервер Б".
* *Плюсы:* Очень быстрый, не расшифровывает трафик.
* *Минусы:* Не видит URL, Cookies, Headers. Нельзя сделать "Sticky Session по куке" или "роутинг /api на один сервис, а /images на другой".
* *Пример:* AWS Network Load Balancer, LVS.


* **L7 Load Balancer (Application Level):**
* Работает с HTTP-запросами.
* *Принцип:* Принимает запрос, **расшифровывает TLS**, читает заголовки, принимает решение, **шифрует обратно** (или нет) и шлет на бэкенд.
* *Плюсы:* Умный роутинг (по URL, User-Agent, Cookie), терминация SSL.
* *Минусы:* Медленнее (нагрузка на CPU из-за шифрования).
* *Пример:* Nginx, HAProxy, AWS ALB.



---

### 3. TCP vs UDP

**Ключевые отличия:**

| Характеристика | TCP (Transmission Control Protocol) | UDP (User Datagram Protocol) |
| --- | --- | --- |
| **Гарантия доставки** | **Да** (ACK-пакеты). Если пакет потерялся, TCP отправит его снова (Retransmission). | **Нет**. "Выстрелил и забыл". Потерялся — ну и ладно. |
| **Порядок (Ordering)** | **Да** (Sequence Numbers). Соберет файл в правильном порядке. | **Нет**. Пакеты могут прийти вразнобой. |
| **Соединение** | Heavyweight (Handshake, состояние сессии). | Lightweight (Без установки соединения). |
| **Использование** | Web (HTTP), Email, FTP, SSH. | Видео-стриминг, Онлайн-игры (важен пинг, а не потеря кадра), DNS. |

**TCP Handshake (3-way):**

1. **SYN** (Client): "Хочу подключиться, мой номер последовательности X".
2. **SYN-ACK** (Server): "Ок, слышу тебя. Мой номер Y, твой X+1 принял".
3. **ACK** (Client): "Ок, твой Y+1 принял. Соединение установлено".

* *Влияние на RTT:* Чтобы просто начать передавать данные, нужно потратить **1.5 RTT**. Это задержка перед стартом.

---

### 4. Эволюция HTTP

Это вопрос про **оптимизацию производительности**.

**HTTP 1.1:**

* Текстовый протокол.
* **Keep-Alive:** Позволил не закрывать TCP-соединение после каждого файла (раньше на каждую картинку был новый Handshake).
* **Проблема Head-of-Line (HoL) Blocking:** В рамках одного соединения запросы идут строго по очереди. Если первый запрос (тяжелый отчет) завис, картинки за ним не загрузятся, пока он не пройдет.

**HTTP 2:**

* **Бинарный протокол** (эффективнее парсинг).
* **Multiplexing (Мультиплексирование):**
* Внутри одного TCP-соединения создаются виртуальные "стримы".
* Можно отправить 10 запросов параллельно и получать кусочки ответов вперемешку. Нет блокировки на уровне приложения.


* **HPACK:** Сжатие заголовков (не шлем `User-Agent` каждый раз, шлем только diff).
* **Проблема:** Остался **TCP HoL Blocking**. Если на уровне TCP потерялся один пакет, ОС остановит все стримы (даже успешные), пока не перешлет потерянный пакет (TCP гарантирует порядок).

**HTTP 3 (QUIC):**

* Работает поверх **UDP**.
* Решает проблему TCP HoL Blocking: потеря пакета в одном стриме не тормозит другие стримы.
* **Faster Handshake:** Соединение + Шифрование устанавливаются за **0-RTT** (если уже общались) или 1-RTT.

---

### 5. HTTPS и TLS

**TLS Handshake (Рукопожатие):**
Самая дорогая часть соединения.

1. Client Hello (поддерживаемые шифры).
2. Server Hello + **Certificate** (публичный ключ).
3. Клиент проверяет сертификат в центре сертификации (CA).
4. **Key Exchange:** Клиент генерирует сессионный ключ, шифрует его публичным ключом сервера и отправляет.
5. Теперь у обоих есть общий секретный ключ.

**Типы шифрования:**

* **Асимметричное (Public/Private Key):** Используется **только** для Handshake (обмена ключами). Оно очень медленное (CPU heavy).
* **Симметричное (AES, ChaCha20):** Используется для передачи **самих данных** (HTML, JSON). Оно очень быстрое.

**TLS 1.2 vs 1.3:**

* **TLS 1.2:** Требует 2 RTT для установки соединения.
* **TLS 1.3:** Упростили алгоритм, требует **1 RTT**. Ускорение загрузки сайтов на 30-50%.

---

### 6. DNS (Domain Name System)

**Почему UDP?**

* Запросы маленькие (обычно влезают в один пакет 512 байт).
* Скорость важнее надежности (если потерялся — клиент просто спросит еще раз через 1 сек).
* TCP используется только для Zone Transfer (синхронизация между DNS-серверами) или если ответ огромный (DNSSEC).

**Типы записей:**

* **A:** Домен -> IPv4 (`1.2.3.4`).
* **AAAA:** Домен -> IPv6.
* **CNAME (Canonical Name):** Алиас. `www.google.com` -> `google.com`. (В CNAME нельзя хранить IP, только другое имя).
* **MX:** Почтовый сервер для домена.

---